// all routes are prefixed with /api/users.

import { } from '../controllers/videosController.js';

import express from 'express';

const videosRouter = express.Router();


export default videosRouter;
